# Confidential source material — brand PDFs and the Indish eVolt investor
# deck (per-machine returns, capital requirements, phase-wise projections).
# This repository is public: keep these out of it. Everything the site needs
# has already been extracted into src/assets/.
assets/documents/

# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment variables
.env
.env.production
# macOS / editor
.DS_Store
Thumbs.db
.vscode/
!.vscode/extensions.json
