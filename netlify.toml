# Netlify build configuration for the Astro static site.
# This overrides any dashboard settings, so the deploy always builds correctly.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Astro 7 requires Node >= 22.12.0.
  NODE_VERSION = "22"
